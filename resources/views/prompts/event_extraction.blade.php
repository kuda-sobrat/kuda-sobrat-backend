Пожалуйста, проанализируй нижеприведенный текст и выполни следующие задачи:

1. Определи, содержит ли текст информацию о скором мероприятии или нескольких мероприятиях. Если да, установи флаг `"is_event": true`; если нет, установи `"is_event": false`.

2. Если `"is_event": true`, извлеки информацию о каждом мероприятии и верни данные в формате JSON с полями:

- `name`: Название мероприятия.
- `description`: Описание мероприятия.
- `start_datetime`: Дата и время начала мероприятия в формате ISO 8601 (YYYY-MM-DDTHH:MM:SS±HH:MM).
- `end_datetime`: Дата и время окончания мероприятия в формате ISO 8601 или `null`, если неизвестно.
- `location`: Место проведения мероприятия.
- `is_paid`: `true` - если платное, `false` - если мероприятие бесплатное.
- `price`: Цена мероприятия с указанием валюты (формат поля - строка) или `null`, если неизвестно.

3. Если мероприятий несколько, верни массив с информацией о каждом мероприятии в поле `"events"`.
4. Дату начала определи, опираясь на текущий год: {{ $nowYear }} и месяц: {{ $nowMonth }}

**Важно:** Верни результат **только** в формате JSON, без дополнительного текста или комментариев.

**Текст для анализа:**
{!! $inputText !!}
